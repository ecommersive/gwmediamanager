import React from 'react'

const ViewList = ({currentData}) => {
  return (
    <div>
    {(currentData === 'Playlist Schedule' || currentData === 'Ads Schedule')
        
        
    }
    </div>
  )
  
}

export default ViewList